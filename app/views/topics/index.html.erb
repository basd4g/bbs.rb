<h1>Listing topics</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>description</th>
    <th></th>
  </tr>
 
  <% @topics.each do |topic| %>
    <tr>
      <td><%= topic.title %></td>
      <td><%= topic.description %></td>
      <td><%= link_to 'Show', topic_path(topic) %></td>
    </tr>
  <% end %>
</table>
